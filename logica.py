"""
Metodos de los modulos  
hexadecimal:
	es_hexadecimal(numero)->recibe cadena,devuelve verdadero o falso 
	decimal_hexadecimal(numero)->recibe decimal,devuelve cadena 
	hexadecimal_decimal(hexadecimal)->recibe cadena, devuelve decimal
	
binario:
	es_binario(numero)->recibe cadena,devuelve verdadero o falso
	binario_decimal(numero)->recibe cadena,devuelve decimal 
	decimal_binario(numero)->recibe decimal,devuelve cadena 
	
octal: 
	es_octal(numero)->recibe cadena,devuelve verdadero o falso 
	decimal_octal(numero)->recibe decimal,devuleve cadena 
	octal_decimal(numero)->recibe cadena,devuelve decimal	


(dec)->decimal 
(oct)->octal 
(hex)->hexadecimal
(bin)->binario
""""

#cargamos los modulos de los sistemas para realizar las conversiones 
from modulos import binarios as bn   
from modulos import octal as oct 
from modulos import hexadecimal as hexa


